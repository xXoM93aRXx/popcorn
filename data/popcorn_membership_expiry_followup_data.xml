<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        
        <!-- Scheduled Action: Create follow-up activities for expiring memberships -->
        <record id="ir_cron_membership_expiry_followup" model="ir.cron">
            <field name="name">Popcorn: Create Membership Expiry Follow-up Activities</field>
            <field name="model_id" ref="model_popcorn_membership"/>
            <field name="state">code</field>
            <field name="code">model._cron_create_expiry_followup_activities()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
            <field name="user_id" ref="base.user_root"/>
            <field name="priority">10</field>
        </record>
        
    </data>
</odoo>

